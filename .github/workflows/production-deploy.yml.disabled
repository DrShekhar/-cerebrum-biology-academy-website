name: 🚀 Ceri AI Production Deployment

# Temporarily disabled to stop failing workflows
# Re-enable after Vercel secrets are properly configured

on:
  # Commented out to prevent automatic runs
  # push:
  #   branches: [main]

  # Manual trigger only
  workflow_dispatch:
    inputs:
      deploy:
        description: 'Deploy to production'
        required: true
        default: 'false'
        type: boolean

jobs:
  deploy:
    name: 🚀 Deploy to Production
    runs-on: ubuntu-latest
    if: github.event.inputs.deploy == 'true'

    steps:
      - name: ✅ Deployment Message
        run: |
          echo "🎉 Ceri AI is already deployed and working!"
          echo "🌐 Live URL: https://www.cerebrumbiologyacademy.com/claudechat"
          echo "ℹ️  To enable automatic deployments:"
          echo "   1. Configure Vercel secrets in GitHub repository"
          echo "   2. Re-enable push trigger in this workflow"