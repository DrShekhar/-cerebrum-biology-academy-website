/**
 * Master free-trial length for new signups (phone OTP, email, OAuth).
 * Owner decision Jul 5, 2026: 15 days (was 7). UI copy that mentions the
 * trial length should import this rather than hardcode a number.
 */
export const TRIAL_DAYS = 15
