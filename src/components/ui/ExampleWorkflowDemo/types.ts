export interface ExampleWorkflowDemoProps {
  className?: string
  children?: React.ReactNode
}

export interface ExampleWorkflowDemoState {
  // Add state interface if needed
}

export interface ExampleWorkflowDemoContext {
  // Add context interface if needed
}
